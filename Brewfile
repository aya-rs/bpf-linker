# The following packages provide static libraries of LLVM dependencies.

# We use it as a linker during LLVM builds (as it's significantly faster than
# Apple ld64) and it's not available on macOS runners by default.
brew "lld"
# This package provides static libc++.
brew "llvm"
brew "zlib"
brew "zstd"
