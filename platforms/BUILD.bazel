# rules_rs requires an explicit Windows ABI; use MSVC to match prebuilt rustc.
platform(
    name = "local_windows_msvc",
    constraint_values = [
        "@llvm//constraints/windows/abi:msvc",
    ],
    parents = ["@platforms//host"],
)
